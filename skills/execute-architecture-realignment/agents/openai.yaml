interface:
  display_name: "[execute] 구조 정렬"
  short_description: "동작은 유지하고 모듈 이름, 책임, 의존 방향을 한 범위 안에서 정리합니다."
  default_prompt: "$execute-architecture-realignment 스킬로 동작은 유지하고 모듈 이름, 책임, 의존 방향을 한 범위 안에서 정리합니다."
policy:
  allow_implicit_invocation: true
