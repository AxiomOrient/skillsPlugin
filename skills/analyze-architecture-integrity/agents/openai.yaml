interface:
  display_name: "[analyze] 코드 구조 진단"
  short_description: "모듈 경계, 이름, 의존 방향이 꼬였는지 구현 전에 확인합니다."
  default_prompt: "$analyze-architecture-integrity 스킬로 모듈 경계, 이름, 의존 방향이 꼬였는지 구현 전에 확인합니다."
policy:
  allow_implicit_invocation: true
