interface:
  display_name: "[release] 패키지 릴리스 배포"
  short_description: "승인된 릴리스를 커밋, 푸시, 게시 단계로 안전하게 실행합니다."
  default_prompt: "$release-publish-package 스킬로 승인된 릴리스를 커밋, 푸시, 게시 단계로 안전하게 실행합니다."
policy:
  allow_implicit_invocation: true
