interface:
  display_name: "[execute] 제품 완성 구현"
  short_description: "계획과 현재 코드 상태를 보고 남은 핵심 구현을 끝까지 처리합니다."
  default_prompt: "$execute-product-completion 스킬로 계획과 현재 코드 상태를 보고 남은 핵심 구현을 끝까지 처리합니다."
policy:
  allow_implicit_invocation: true
