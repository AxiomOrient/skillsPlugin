# Akasha Intake Routing

Mobile-safe Akasha routing skills for narrowing user requests before work starts.

This pack requires AMA to register these Swift host intents:

- `akasha.scope_lock`
- `akasha.route_lock`
- `akasha.request_route`

The pack contains no script files.
